# 工作包 SITE-2：首页双效果主题预览增强

## 基本信息

- 日期：2026-07-23
- 状态：已完成
- 对应执行计划工作包：SITE-2

## 本次实现内容

- 在首页组件墙注册 Matrix Effect 预览，并提升为第一个 `wide` 特色卡片。
- 同卡并列展示圆点矩阵与 ASCII 字符矩阵，两者共享稳定的柔和流动光团 Source。
- 新增 SSR 稳定的深浅主题配色联动，并限制首页动画的帧率、DPR 和网格上限。
- 完成桌面、移动端、深色、浅色与动态帧变化验收。

## 修改文件

- `components/home/home-component-previews.tsx`
- `lib/home-component-preview-config.ts`
- `docs/设计并开发MatrixEffect组件/MatrixEffect组件开发设计文档.md`
- `docs/设计并开发MatrixEffect组件/MatrixEffect组件执行计划.md`
- `docs/设计并开发MatrixEffect组件/feat/2026-07-23_MatrixEffect_首页双效果主题预览增强.md`
- `docs/设计并开发MatrixEffect组件/MatrixEffect组件实施记录/2026-07-23_SITE-2_首页双效果预览增强.md`

## 接口或数据结构变化

- 新增站点内部 `MatrixEffectPreview`、首页配色常量与预览配置项。
- MatrixEffect 公共 API、Registry item、npm 依赖和锁文件均无变化。

## 与设计文档的一致性

- 是否偏离设计：否。
- 预览复用现有 Source/Renderer 契约和主题系统，没有把首页展示参数扩大成公共组件默认值。

## 验证结果

执行命令：

```text
pnpm --version
pnpm update-registry
pnpm update-registry:dry
pnpm exec eslint components/home/home-component-previews.tsx lib/home-component-preview-config.ts
pnpm exec tsc --noEmit --pretty false
pnpm lint
pnpm build
pnpm exec prettier --check <本次交付文件>
git diff --check
```

结果：

- pnpm `8.7.0`、Registry 14 个 item 零变化、ESLint、TypeScript 与 21 个静态页面生产构建通过。
- 1280px 视口下 Matrix Effect 与 Dot Glass 同为 `600 x 426` CSS px，内部两个 Canvas 均为 `275 x 228` CSS px。
- `1440 x 900` 视口下 Matrix Effect 为 `675.7 x 423.3` CSS px，内部两个 Canvas 均约为 `313 x 226.6` CSS px，与 Dot Glass 保持同级宽卡尺寸。
- 圆点与 ASCII 的间隔帧 SHA-256 哈希均变化；深浅主题实际背景分别为 `rgb(9, 9, 11)` 与 `rgb(246, 246, 246)`。
- `390 x 844` 视口下两个 `322 x 180.25` Canvas 上下排列，无重叠或横向溢出。
- 浏览器控制台无 warning/error，验收标签、视口覆盖和开发服务已清理。

## 未完成事项

- 无。

## 下一步建议

- 继续执行 QA-2 中剩余的高 DPR、压力降档、Reduced Motion 运行时切换与反复挂载清理验收。
