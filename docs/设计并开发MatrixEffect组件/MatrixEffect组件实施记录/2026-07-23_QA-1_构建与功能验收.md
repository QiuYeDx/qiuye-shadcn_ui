# 工作包 QA-1：构建、Registry 与功能验收

## 基本信息

- 日期：2026-07-23
- 状态：已完成
- 对应执行计划工作包：`QA-1 构建、Registry 与功能验收`

## 本次实现内容

- 使用固定版本 `shadcn@4.14.0` 从本地 HTTP Registry URL 安装 `matrix-effect`，以独立临时消费项目核对真实分发路径，而不是只检查仓库内 JSON。
- 验证七个分发源码文件、Registry content、相对 import 闭包、总 manifest、公共导出和严格 TypeScript 消费契约；确认 Dot/ASCII 预设只接受 `backgroundColor`，核心使用 `clearColor`。
- 创建并在验收后删除一次性浏览器路由，以 44 项断言覆盖静态图片、外部 Canvas、程序化 Source、Mapper/Transform/Renderer 顺序、`previousValues`、ref、invalidate 合并、配置身份失效、Dot/ASCII auto/fixed grid、错误恢复、零尺寸、非法值、无障碍、fallback、事务性保帧和快速 Source 切换。
- 在正式 `/components/matrix-effect/` 页面依次验证圆点、ASCII 和自定义 Demo 的非背景像素、量化色彩与短时动态/静态行为，并检查 Registry 与示例资产路由。
- 独立源码复审发现 JSDoc 示例内联创建 Source 与既有身份契约冲突；创建 `fix/2026-07-23_MatrixEffect_Source身份JSDoc示例修复.md`，补充稳定身份说明、修正两个示例并重新同步 Registry。

## 修改文件

- `components/qiuye-ui/matrix-effect/matrix-effect.tsx`
- `components/qiuye-ui/matrix-effect/presets.tsx`
- `components/qiuye-ui/matrix-effect/types.ts`
- `public/registry/matrix-effect.json`
- `docs/设计并开发MatrixEffect组件/MatrixEffect组件开发设计文档.md`
- `docs/设计并开发MatrixEffect组件/MatrixEffect组件执行计划.md`
- `docs/设计并开发MatrixEffect组件/fix/2026-07-23_MatrixEffect_Source身份JSDoc示例修复.md`
- `docs/设计并开发MatrixEffect组件/MatrixEffect组件实施记录/2026-07-23_QA-1_构建与功能验收.md`

一次性 `app/matrix-effect-qa1-test/` 仅用于浏览器验收，完成后已删除，不属于交付文件。

## 接口或数据结构变化

- 无公共 API、运行时行为或数据结构变化。
- `MatrixEffectProps.source` 的 JSDoc 明确 descriptor 按身份切换且应视为不可变配置；核心与 ASCII 示例改用稳定 Source 常量。
- 公共入口保持 44 个导出：12 个运行时导出、32 个类型导出。
- Registry item 继续保持七个文件，`dependencies` 与 `registryDependencies` 均为空。

## 与设计文档的一致性

- 是否偏离设计：否。
- Source 稳定身份修正只是把设计文档已有的不可变配置约束补充到公共 JSDoc，没有改变 Source epoch 或失效规则。
- QA-1 只关闭安装、静态正确性和有界功能路径；CPU 压力、完整 DPR/视口矩阵、暂停、Reduced Motion、反复清理和 React commit 检查仍保留给 QA-2。

## 验证结果

执行命令：

```text
pnpm --version
/usr/bin/python3 -m http.server 4175 --bind 127.0.0.1 --directory public
pnpm dlx shadcn@4.14.0 add http://127.0.0.1:4175/registry/matrix-effect.json --yes
node verify.mjs
./node_modules/.bin/tsc -p tsconfig.json --noEmit --pretty false
pnpm update-registry
pnpm update-registry:dry
pnpm lint
pnpm exec tsc --noEmit --pretty false
./node_modules/.bin/prettier --check <MatrixEffect QA-1 交付源码、Registry 与文档>
pnpm dev
pnpm build
cmp -s public/registry/matrix-effect.json out/registry/matrix-effect.json
cmp -s public/examples/matrix-effect/source.webp out/examples/matrix-effect/source.webp
git diff --check
```

浏览器验收路径：

```text
http://localhost:3000/matrix-effect-qa1-test/（一次性路由，已删除）
http://localhost:3000/components/matrix-effect/
http://localhost:3000/registry/matrix-effect.json
http://localhost:3000/examples/matrix-effect/source.webp
```

结果：

- 当前 pnpm 为 8.7.0；真实 shadcn 安装在全新临时项目中创建恰好七个 MatrixEffect 文件，安装内容、Registry content 与仓库源码逐字一致。
- 严格 TypeScript 消费夹具通过；12 个运行时导出与 32 个类型导出全部可用，Dot/ASCII 的非法 `clearColor` 由 `@ts-expect-error` 精确覆盖。
- Registry/import/manifest 断言通过：七文件相对 import 闭包完整，空依赖保持不变，总 manifest 文件路径一致且不携带源码 content；最终 dry-run 扫描 14 个 item，0 个待更新文件。
- 自运行浏览器夹具七组共 44 项断言全部通过：图片静置不重绘、五次 invalidate 合并为一帧、Resize 单帧重绘；外部 Canvas 仅在 invalidate 后更新；管线顺序、历史、prepare/dispose 与身份失效正确；Dot/ASCII 网格分别得到 `24x12`、`24x7`、`100x50`、`100x30`；错误、恢复、ARIA、fallback、保帧和快速切换均符合契约。
- 正式页面在 1280x720 视口验收通过。圆点 Canvas 截图区域为 888x498，具有 32 种量化色，1.8 秒内 178732 个像素变化；ASCII 具有 131 种量化色、10280 个非背景采样点，0.9 秒逐像素稳定；自定义场景具有 135 种量化色、23523 个非背景采样点，0.9 秒逐像素稳定。
- 正式页面三种 Demo 均非全透明或纯背景，控制切换成功，没有新增控制台 error；一次性夹具中的空字符集、非法数值告警与缺图请求属于预期测试输入。
- 全量 ESLint、TypeScript、定向 Prettier、`git diff --check` 和生产构建通过；Next 生成 21 个静态页面。
- `out/components/matrix-effect/index.html` 存在且包含 Matrix Effect；导出的 Registry JSON 与源 item 完全一致，导出的 WebP 与源资产逐字节一致。
- `package.json` 与 `pnpm-lock.yaml` 无差异。一次性路由和空测试目录已删除，浏览器标签、Next 开发服务与 Registry 静态服务均已关闭，端口 3000 与 4175 已确认释放。
- 两轮独立只读审计覆盖安装、测试矩阵、源码契约与文档交接；除已修复的 Source 身份 JSDoc 问题外，未发现 QA-1 遗留缺陷。

## 未完成事项

- QA-2 仍需覆盖 DPR 1/2/高 DPR 上限、横向/方形/纵向容器和桌面/移动多视口视觉。
- QA-2 仍需使用 Chrome 4x/6x CPU throttling 验证 auto FPS 在压力下从 60 降到 30、稳定不抖动，并在健康窗口与冷却时间后回升。
- QA-2 仍需验证后台/离屏暂停、运行时 Reduced Motion、反复挂载卸载后的 Observer/rAF/图片资源清理，以及连续动画不触发每帧 React commit。

## 下一步建议

- 下一工作包认领 `QA-2 视觉、响应式、性能与生命周期验收`。
- 读取本记录并复用已通过的 Registry 安装与功能基线，把验收集中在性能、暂停、清理和多视口风险，不重复扩大公共 API。
