# 工作包 FE-1：快速上手页面重构

## 基本信息

- 日期：2026-07-07
- 状态：已完成
- 对应执行计划工作包：PRE-1、FE-1、QA-1

## 本次实现内容

- 将旧 `/cli` 页面彻底替换为“QiuYe UI 快速开始”页面。
- 保留 `/cli` 路由兼容旧链接，但页面心智从 CLI 文档改为用户接入路径。
- 新页面包含首屏行动区、包管理器切换、四步接入、组件速览、URL 安装兜底、接入检查、Registry 配置和 MCP 可选入口。
- 组件速览改为基于 `getAllComponents()` 读取 registry 元数据，不再维护旧硬编码组件清单。
- 更新站点导航、首页 CTA、遗留 Header/Sidebar 中指向 `/cli` 的显示文案。
- 清理 `components/header.tsx` 中因本次触碰文件而暴露的未使用 import/常量 warning。
- 移动端视觉检查发现长命令撑大首屏宽度，已通过 `min-w-0`、`max-w-full` 和移动端按钮宽度约束修复。

## 修改文件

- `app/cli/page.tsx`
- `components/site/site-nav.ts`
- `components/site/site-header.tsx`
- `components/home/home-hero.tsx`
- `components/home/home-cta.tsx`
- `components/app-sidebar.tsx`
- `components/header.tsx`
- `docs/重构快速上手页面/快速上手页面重构开发设计文档.md`
- `docs/重构快速上手页面/快速上手页面重构执行计划.md`
- `docs/重构快速上手页面/快速上手页面重构实施记录/2026-07-07_FE-1_快速上手页面重构.md`

## 接口或数据结构变化

- 无新增接口。
- 无 registry 数据结构变化。
- `/cli` 路由保持不变。

## 验证结果

执行命令：

```text
node_modules/.bin/prettier --write app/cli/page.tsx components/site/site-nav.ts components/site/site-header.tsx components/app-sidebar.tsx components/header.tsx components/home/home-hero.tsx components/home/home-cta.tsx docs/重构快速上手页面/快速上手页面重构开发设计文档.md docs/重构快速上手页面/快速上手页面重构执行计划.md
node_modules/.bin/eslint app/cli/page.tsx components/site/site-nav.ts components/site/site-header.tsx components/app-sidebar.tsx components/header.tsx components/home/home-hero.tsx components/home/home-cta.tsx
node_modules/.bin/next build
```

结果：

- Prettier 执行成功。
- ESLint 执行成功，0 error，0 warning。
- Next build 执行成功，`/cli` 静态页面生成成功。
- 使用临时 `next dev --hostname 127.0.0.1 --port 3000` 做视觉检查后已关闭进程。
- 桌面 1280x720：`h1` 为 `QiuYe UI 快速开始`，导航包含 `Quick Start`，横向溢出为 0。
- 移动 390x844：`h1` 为 `QiuYe UI 快速开始`，横向溢出为 0，首屏按钮宽度回到容器内。
- 3000 端口已确认无遗留进程。

## 未完成事项

- 无。

## 下一步建议

- 如后续要进一步强化快速上手体验，可为 `/components/[id]` 增加从快速开始页传入的推荐组件锚点或安装提示，但不属于本次重构范围。
