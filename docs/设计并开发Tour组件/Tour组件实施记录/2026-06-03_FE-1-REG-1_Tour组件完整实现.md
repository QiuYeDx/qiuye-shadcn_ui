# 工作包 FE-1 / FE-2 / REG-1 / QA-1：Tour 组件完整实现

## 基本信息

- 日期：2026-06-03
- 状态：已完成
- 对应执行计划工作包：FE-1 主组件实现、FE-2 Demo 与详情页预览、REG-1 Registry 与站点接入、QA-1 本地验证

## 本次实现内容

- 新增 `Tour` 主组件，支持 selector / HTMLElement / function target、受控与非受控 open/currentStep、步骤进度、上一步、下一步、跳过、完成。
- 使用 `motion/react` 的 `LayoutGroup`、`AnimatePresence`、`layoutId` 实现 spotlight 与 popover 迁移动画。
- 实现遮罩聚焦区域、透明命中层、目标点击控制、自动滚动、resize/scroll/visualViewport 更新、popover placement fallback 与 viewport clamp。
- 实现键盘操作：Escape 跳过、ArrowLeft 上一步、ArrowRight 下一步，并避开输入框与 contenteditable。
- 新增完整 demo 与快速预览 demo，覆盖多步骤、popover placement、目标 resize 场景。
- 接入组件详情页、组件常量、基础用法、registry 元信息、CLI 可用组件列表。
- 新增 `public/registry/tour.json` 并运行 registry 生成脚本同步 `content` 与 `registry.json`。

## 修改文件

- `components/qiuye-ui/tour.tsx`
- `components/qiuye-ui/demos/tour-demo.tsx`
- `app/components/[id]/page.tsx`
- `app/components/[id]/simple-demos.tsx`
- `app/cli/page.tsx`
- `lib/component-constants.ts`
- `lib/registry.ts`
- `public/registry/tour.json`
- `public/registry/registry.json`
- `docs/设计并开发Tour组件/Tour组件开发设计文档.md`
- `docs/设计并开发Tour组件/Tour组件执行计划.md`

## 接口或数据结构变化

- 新增 `ComponentId.TOUR = "tour"`。
- 新增导出类型：`TourPlacement`、`TourAlign`、`TourStep`、`TourRenderContext`、`TourProps`。
- 新增导出组件：`Tour`。
- 新增 registry item：`@qiuye-ui/tour`。

## 验证结果

执行命令：

```text
pnpm update-registry
pnpm lint
pnpm build
pnpm update-registry:dry
```

结果：

- `pnpm update-registry` 成功，`public/registry/tour.json` 已写入组件源码 content，`public/registry/registry.json` items 为 10。
- `pnpm lint` 成功，无错误；仍有项目既有未使用 import warning。
- `pnpm build` 成功完成静态构建；仍有同一批既有未使用 import warning。
- `pnpm update-registry:dry` 成功，实际将要更新 0。

## 未完成事项

- 无代码阻塞事项。
- 浏览器交互由用户在页面手动点击检查，本次未继续自动浏览器测试。

## 下一步建议

- 手动检查 `/components/tour` 的完整 demo：Start tour、Next、Previous、Skip、Done、Resize target。
- 若需要更强本地化能力，可后续增加 `labels` prop 支持按钮文案自定义。
